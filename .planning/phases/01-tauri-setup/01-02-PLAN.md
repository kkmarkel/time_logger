---
phase: 01-tauri-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/verify-tauri-prereqs.sh
  - scripts/verify-phase1-gaps.sh
  - package.json
  - README.md
  - .planning/phases/01-tauri-setup/01-GAP-CLOSURE-EVIDENCE.md
autonomous: false
requirements:
  - TAURI-02
  - TAURI-03
gap_closure: true
must_haves:
  truths:
    - "`npm run tauri dev` starts without errors on a prerequisite-complete host"
    - "The Tauri app window opens from the dev command"
    - "`npm run tauri build` produces `src-tauri/target/release/bundle/nsis/time_logger.exe`"
  artifacts:
    - path: "scripts/verify-tauri-prereqs.sh"
      provides: "Deterministic prerequisite gate for linker + WebKit dependencies"
      contains: "webkit2gtk-4.1"
    - path: "scripts/verify-phase1-gaps.sh"
      provides: "One-command gap verification for dev and build truths"
      contains: "npm run tauri build"
    - path: ".planning/phases/01-tauri-setup/01-GAP-CLOSURE-EVIDENCE.md"
      provides: "Recorded command evidence for TAURI-02 and TAURI-03"
      contains: "time_logger.exe"
    - path: "src-tauri/target/release/bundle/nsis/time_logger.exe"
      provides: "Required Windows bundle artifact"
      exists: true
  key_links:
    - from: "package.json"
      to: "scripts/verify-tauri-prereqs.sh"
      via: "npm script tauri:prereqs"
      pattern: "tauri:prereqs"
    - from: "package.json"
      to: "scripts/verify-phase1-gaps.sh"
      via: "npm script phase1:verify"
      pattern: "phase1:verify"
    - from: "scripts/verify-phase1-gaps.sh"
      to: "src-tauri/target/release/bundle/nsis/time_logger.exe"
      via: "artifact existence assertion"
      pattern: "time_logger\\.exe"
---

<objective>
Close Phase 1 verification gaps by adding deterministic prerequisite checks, rerunning dev/build commands on a capable host, and recording evidence for TAURI-02/TAURI-03.

Purpose: convert environment-dependent failures into reproducible pass/fail gates and documented closure evidence.
Output: prerequisite checker scripts, npm verification commands, and a phase evidence artifact.
</objective>

<execution_context>
@/home/kkmarkel/git-kkmarkel/time_logger/.opencode/get-shit-done/workflows/execute-plan.md
@/home/kkmarkel/git-kkmarkel/time_logger/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tauri-setup/01-CONTEXT.md
@.planning/phases/01-tauri-setup/01-VERIFICATION.md
@.planning/phases/01-tauri-setup/01-01-SUMMARY.md
@package.json
@src-tauri/tauri.conf.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prerequisite + phase gap verification harness (per D-01/D-13)</name>
  <read_first>package.json, README.md, src-tauri/tauri.conf.json, .planning/phases/01-tauri-setup/01-VERIFICATION.md</read_first>
  <files>scripts/verify-tauri-prereqs.sh, scripts/verify-phase1-gaps.sh, package.json, README.md</files>
  <action>Create `scripts/verify-tauri-prereqs.sh` with `set -euo pipefail` that runs `npm run tauri -- info`, checks for `cc` in PATH, and fails with explicit install guidance when output contains missing Linux prerequisites (`webkit2gtk-4.1`, `rsvg2`) or linker issues. Add exact Ubuntu/Debian install command `sudo apt update && sudo apt install -y build-essential libwebkit2gtk-4.1-dev librsvg2-dev patchelf`. Create `scripts/verify-phase1-gaps.sh` that runs `npm run tauri:prereqs`, starts `npm run tauri dev` with bounded timeout for startup proof, then runs `npm run tauri build`, and asserts `src-tauri/target/release/bundle/nsis/time_logger.exe` exists. Add npm scripts `tauri:prereqs` and `phase1:verify` in `package.json`. Update README with a “Phase 1 Verification” section that lists the two npm commands and expected artifact path.</action>
  <acceptance_criteria>
    - `package.json` contains `"tauri:prereqs"` and `"phase1:verify"` script keys.
    - `scripts/verify-tauri-prereqs.sh` contains `webkit2gtk-4.1`, `rsvg2`, and `build-essential` strings.
    - `scripts/verify-phase1-gaps.sh` contains `npm run tauri dev`, `npm run tauri build`, and `time_logger.exe` checks.
    - `README.md` contains heading `## Phase 1 Verification` and the command `npm run phase1:verify`.
  </acceptance_criteria>
  <verify>
    <automated>npm run tauri:prereqs || true</automated>
  </verify>
  <done>Repository has a concrete, repeatable command path for all previously failed truths in TAURI-02/TAURI-03.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run closure verification on a prerequisite-complete host</name>
  <read_first>README.md, scripts/verify-tauri-prereqs.sh, scripts/verify-phase1-gaps.sh, .planning/phases/01-tauri-setup/01-VERIFICATION.md</read_first>
  <files>.planning/phases/01-tauri-setup/01-GAP-CLOSURE-EVIDENCE.md</files>
  <decision>This step requires host-level package/toolchain setup and a build-capable environment that this workspace may not control.</decision>
  <context>Phase 1 gaps explicitly require (a) satisfied system prerequisites and (b) successful `.exe` artifact generation. These cannot be inferred from static config and must be proven by command output.</context>
  <action>Execute the exact commands documented in README (`npm run tauri:prereqs` and `npm run phase1:verify`) on a host where system dependencies can be installed. Persist terminal output snippets and exit codes so Task 3 can record reproducible evidence for TAURI-02/TAURI-03 closure.</action>
  <how-to-verify>
    1. On a host where you can install system dependencies, run `npm run tauri:prereqs` and confirm exit code 0.
    2. Run `npm run phase1:verify` and wait for both dev startup check and build step to complete.
    3. Confirm file exists: `src-tauri/target/release/bundle/nsis/time_logger.exe`.
    4. Capture command outputs and continue with exact pass/fail details.
  </how-to-verify>
  <acceptance_criteria>
    - `npm run tauri:prereqs` exits 0 on the verification host.
    - `npm run phase1:verify` exits 0.
    - `src-tauri/target/release/bundle/nsis/time_logger.exe` exists after command completion.
  </acceptance_criteria>
  <verify>
    <automated>npm run phase1:verify</automated>
  </verify>
  <done>Verification commands succeed on a capable host and produce the expected executable artifact path for evidence capture.</done>
  <resume-signal>Type "done" with command output snippets (or provide exact failure output).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record closure evidence artifact for re-verification</name>
  <read_first>.planning/phases/01-tauri-setup/01-VERIFICATION.md, .planning/phases/01-tauri-setup/01-01-SUMMARY.md</read_first>
  <files>.planning/phases/01-tauri-setup/01-GAP-CLOSURE-EVIDENCE.md</files>
  <action>Create `.planning/phases/01-tauri-setup/01-GAP-CLOSURE-EVIDENCE.md` with sections: `## Commands Executed`, `## TAURI-02 Evidence`, `## TAURI-03 Evidence`, and `## Artifact Checksums`. Include the exact command lines used, exit codes, and artifact confirmation line for `src-tauri/target/release/bundle/nsis/time_logger.exe`. Add SHA256 checksum of the exe when available to make verification reproducible.</action>
  <acceptance_criteria>
    - Evidence file exists at `.planning/phases/01-tauri-setup/01-GAP-CLOSURE-EVIDENCE.md`.
    - File contains headings `## TAURI-02 Evidence` and `## TAURI-03 Evidence`.
    - File contains literal path `src-tauri/target/release/bundle/nsis/time_logger.exe`.
    - File contains `sha256` text when exe artifact is present.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-tauri-setup/01-GAP-CLOSURE-EVIDENCE.md</automated>
  </verify>
  <done>Gap closure evidence is ready for `/gsd-verify` to validate TAURI-02 and TAURI-03 without ambiguity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → package manager | System dependency installation uses privileged host commands |
| npm scripts → tauri build toolchain | Build pipeline executes native toolchain and bundler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | `scripts/verify-tauri-prereqs.sh` | mitigate | Pin commands and check explicit prerequisite strings; store script in VCS for reviewable diffs |
| T-01-02-02 | Denial of Service | Host dependency install | accept | Installation may fail on restricted hosts; checkpoint requires explicit failure output and rerun on capable host |
| T-01-02-03 | Repudiation | Gap closure evidence | mitigate | Require command outputs + exit codes + checksum in `01-GAP-CLOSURE-EVIDENCE.md` |
| T-01-02-04 | Elevation of Privilege | `sudo apt install` guidance | accept | Manual step stays user-controlled; no automated privilege escalation in repo scripts |
</threat_model>

<verification>
- Run: `npm run tauri:prereqs`
- Run: `npm run phase1:verify`
- Confirm: `test -f src-tauri/target/release/bundle/nsis/time_logger.exe`
- Confirm: `test -f .planning/phases/01-tauri-setup/01-GAP-CLOSURE-EVIDENCE.md`
</verification>

<success_criteria>
- TAURI-02 has command evidence showing dev startup on a prerequisite-complete host.
- TAURI-03 has command evidence and produced `.exe` artifact at required path.
- Phase has reproducible prerequisite + verification commands documented in-repo.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tauri-setup/01-02-SUMMARY.md`
</output>
