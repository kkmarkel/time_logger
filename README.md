# time_logger
Desktop time logger

## Phase 1 Verification

Run prerequisite validation first:

```bash
npm run tauri:prereqs
```

Run full Phase 1 gap-closure verification:

```bash
npm run phase1:verify
```

Expected build artifact path:

`src-tauri/target/release/bundle/nsis/time_logger.exe`
